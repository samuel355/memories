import React from 'react';
import useStyles from './styles'

const Form = () => {
  const classes = useStyles();
  return (
    <div>
       <h4>Forms are gon be here</h4>
    </div>
  );
}

export default Form;
